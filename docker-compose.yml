services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: cataclysm
      POSTGRES_PASSWORD: cataclysm
      POSTGRES_DB: cataclysm
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cataclysm"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+asyncpg://cataclysm:cataclysm@postgres:5432/cataclysm
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET:-dev-secret-do-not-use-in-production}
      SESSION_DATA_DIR: /app/data/session
    volumes:
      - ./data:/app/data
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
      args:
        BACKEND_URL: http://backend:8000
    ports:
      - "3000:3000"
    environment:
      AUTH_SECRET: dev-secret-do-not-use-in-production
      AUTH_TRUST_HOST: "true"
    depends_on:
      - backend

volumes:
  pgdata:
