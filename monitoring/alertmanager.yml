global:
  resolve_timeout: 5m

route:
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: "default"

receivers:
  - name: "default"
    # Configure your notification channel here:
    # webhook_configs:
    #   - url: "https://hooks.slack.com/..."
    # email_configs:
    #   - to: "your@email.com"
    #     from: "alertmanager@cataclysm.app"
    #     smarthost: "smtp.example.com:587"
