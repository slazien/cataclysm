global:
  resolve_timeout: 5m

route:
  group_by: ["alertname"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  receiver: "ntfy"

receivers:
  - name: "ntfy"
    webhook_configs:
      - url: "https://ntfy.sh/cataclysm-alerts"
        send_resolved: true
