'use client';

/**
 * Deprecated — FloatingToolsMenu now handles Notes on both platforms.
 * Kept as a no-op export so page.tsx doesn't need a diff.
 */
export function FloatingNotesButton() {
  return null;
}
